# Core Web Framework & Server
fastapi>=0.110.0
uvicorn[standard]>=0.28.0

# Form data parsing & request handling
python-multipart>=0.0.9
pydantic>=2.6.0

# PostgreSQL (Neon DB) driver & environment config
psycopg2-binary>=2.9.9
python-dotenv>=1.0.1

# PDF Generation (Pure Python, native across Windows/macOS/Linux)
reportlab>=4.1.0
# Complex-script shaping for reportlab (HarfBuzz); reportlab discovers uharfbuzz
# at runtime and uses it when the paragraph style has shaping=True. Required so
# Devanagari (Hindi) conjuncts and pre-base matras render correctly in the PDFs.
uharfbuzz>=0.40.0
# Optional HTML/CSS settlement-voucher renderer (Jinja2 + Pango/HarfBuzz). Needs
# native libs on first import (Linux: `apt-get install libpango-1.0-0
# libharfbuzz0b`); when those are absent the PDF service degrades to reportlab.
weasyprint>=69.0

# HTTP Client (for external APIs / WhatsApp Webhook triggers)
requests>=2.31.0
httpx>=0.27.0

# HTML Email Templates (Jinja2) for Resend email delivery
jinja2>=3.1.0

# Async Utilities
anyio>=4.3.0
